import { makeStyles } from '@material-ui/core/styles';


export const useItemStyle = makeStyles({
  item: {
    padding: '2px 8px',
  },
  subHeader: {
    fontWeight: 'bold',
    fontSize: '0.9em',
    paddingTop: '3px',
  },
});
