import { makeStyles } from '@material-ui/core/styles';


export const useLabelsContainerStyle = makeStyles({
  container: {
    padding: '2px 8px',
    marginTop: '4px',
    display: 'flex',
    flexWrap: 'wrap',
  },
});
