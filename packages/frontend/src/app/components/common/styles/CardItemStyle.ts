import { makeStyles } from '@material-ui/core/styles';

export const useCardItemStyle = makeStyles({
  item: {
    padding: '5px 8px',
  },
  subHeader: {
    fontWeight: 'bold',
    fontSize: 'small',
  },
});
